# Ядро
google-genai>=1.40         # Nano Banana (Gemini Image) — основной провайдер
openai>=2.0                # GPT Image 2 — провайдер (силён в тексте)
higgsfield-client==0.1.0   # запасной провайдер; API 0.x нестабилен — при апгрейде сверить
Pillow>=10                 # постобработка + мок-заглушки
python-dotenv>=1           # чтение .env
mammoth>=1.6               # .docx → markdown (кроссплатформенно, не нужен textutil)
httpx>=0.27                # скачивание результата по result-URL
python-telegram-bot>=20    # Telegram-слой (нужен только для запуска бота)

# Разработка
pytest>=8
pytest-asyncio>=0.23
